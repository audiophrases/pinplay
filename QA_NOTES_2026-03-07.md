# QA Notes — 2026-03-07

## Batch QA-1 (completed)

### Checks performed
- Syntax checks:
  - `node --check app.js`
  - `node --check play.js`
  - `node --check cloudflare/worker.js`
- Recent high-risk regressions revalidated in code paths:
  - `timeLimit=0` no-limit logic present host/player/worker
  - Puzzle tokens remain visible in bank (disabled, not removed)
  - Type-answer with empty accepted answers routes to teacher-graded flow
  - Correct-answer reveal shown on student side for auto-graded typed questions

### Outcome
- No syntax errors.
- High-risk behavior patches are present in current code.

## Next QA batches (to run)
1. End-to-end multiplayer run (host + 2 students) through all question families.
2. Dedicated poll-mode regression run (bar/cloud/moderation/Other).
3. Mobile-only pass (secondary host + student).
4. Final release-candidate smoke on full template quiz.
