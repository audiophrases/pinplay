Place optional sound files here:

- `hall.mp3` — lobby/hall background loop (teacher create/host page)
- `answering.mp3` — plays when a new question appears
- `answered.mp3` — plays when an answer is submitted/received

If a file is missing, PinPlay will continue silently.
