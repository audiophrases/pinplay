[
  "app.js\u0000aiBtn.textContent = `Grade with AI (this student, ${teacherGradedCount} answer${teacherGradedCount === 1 ? '' : 's'})`;",
  "app.js\u0000alert(`Mic error: ${err?.message || err}`);",
  "app.js\u0000alert(`Recording save failed: ${err.message}`);",
  "app.js\u0000allBtn.textContent = `Apply ${total} incl. overwrites`;",
  "app.js\u0000answer.textContent = `Answer: ${String(it?.answerText || '') || '(blank)'}`;",
  "app.js\u0000applyAssignmentBtn.textContent = `Apply to Assignment (${applyTargetAssignmentCode})`;",
  "app.js\u0000assignmentGradingSummaryEl.textContent = `${title} · No gradable attempts yet.`;",
  "app.js\u0000assignmentGradingSummaryEl.textContent = `${title} · Per-student grading · ${parts.join(' · ')} · Pick a student below.`;",
  "app.js\u0000assignmentGradingSummaryEl.textContent = `Q${Number(qIndex) + 1} · ${items.length} answer${items.length === 1 ? '' : 's'} · ${gradedNow} graded · ${pendingNow} pending · max ${maxPoints} pts`;",
  "app.js\u0000assignmentStatusEl.textContent = `Opened assignment ${codeTrim} in the builder. Edit, then click \"Apply to Assignment\".`;",
  "app.js\u0000card.innerHTML = `<div class=\"row spread gap\"><strong>${escapeHtml(p.name)}</strong><span class=\"small\">#${i + 1}</span></div><div class=\"row gap top-space\">${stateChip}${betChip}</div><div class=\"small\">Score: ${Number(p.score || 0)} · ${status}${betTxt}</div><div class=\"small muted\">Answer: ${escapeHtml(answerText)}</div>`;",
  "app.js\u0000chip.title = `${item.count} vote(s)`;",
  "app.js\u0000counterEl.textContent = `${len} / 10,000 chars`;",
  "app.js\u0000delBtn.title = `Delete ${item.label}`;",
  "app.js\u0000detail.textContent = `Answered: ${answered} · Graded: ${graded} · Pending: ${pending} · Accuracy: ${accuracy}`;",
  "app.js\u0000diffPreview.innerHTML = `<span class=\"small muted\" style=\"font-style:italic;\">(no changes — student sees no correction)</span>`;",
  "app.js\u0000divider.textContent = `— Archived (${archived.length}) —`;",
  "app.js\u0000errBox.textContent = `Failed to load: ${err?.message || err}`;",
  "app.js\u0000errEl.textContent = `Could not load context: ${err.message}`;",
  "app.js\u0000errEl.textContent = `Could not read file: ${err.message}`;",
  "app.js\u0000errEl.textContent = `Loading current grades for cross-check (${qIndexesNeeded.size} question${qIndexesNeeded.size === 1 ? '' : 's'})…`;",
  "app.js\u0000errEl.textContent = `assignmentCode \"${parsed.assignmentCode}\" does not match current assignment ${safeCode}. Open the right assignment first.`;",
  "app.js\u0000finalScoreEl.textContent = `${soloGame.student}, you scored ${soloGame.score} / ${totalPossible}.`;",
  "app.js\u0000gradeBtn.title = `Award full points (${maxPoints})`;",
  "app.js\u0000hostAttemptsSummaryEl.textContent = `Quiz: ${quizTitle} · Showing ${filtered.length}/${students.length} students`;",
  "app.js\u0000if (!confirm(`Delete \"${item.label}\"?`)) return;",
  "app.js\u0000if (!confirm(`Delete ${n} selected attempt${n === 1 ? '' : 's'}? This cannot be undone.`)) return;",
  "app.js\u0000if (!confirm(`Delete ${who}'s attempt? This cannot be undone.`)) return;",
  "app.js\u0000if (!confirm(`Delete assignment ${code}? This cannot be undone.`)) return;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Grade-by-question error: ${err.message}`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Grade-by-student error: ${err.message}`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Grading ${safeAttemptId} · Items: ${items.length}`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Grading error: ${err.message}`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Loading Q${Number(qIndex) + 1}...`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Loading answers for ${studentName || safeAttemptId}...`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Loading answers for Q${Number(qIndex) + 1}...`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Loading grading detail for ${safeAttemptId}...`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Loading per-question overview for ${safeCode}...`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Loading student list for ${safeCode}...`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Nav error: ${err.message}`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `No teacher-graded questions found for ${studentName || safeAttemptId}.`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Overview error: ${err.message}`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Q${Number(qIndex) + 1} is auto-graded — no teacher grading needed.`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Q${Number(qIndex) + 1}: no answers yet.`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Question grading error: ${err.message}`;",
  "app.js\u0000if (assignmentGradingSummaryEl) assignmentGradingSummaryEl.textContent = `Student grading error: ${err.message}`;",
  "app.js\u0000if (assignmentResultsSummaryEl) assignmentResultsSummaryEl.textContent = `Loading results for ${safeCode}...`;",
  "app.js\u0000if (assignmentResultsSummaryEl) assignmentResultsSummaryEl.textContent = `Results error: ${err.message}`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `${msg} · Link: ${link}`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Apply to assignment error: ${err.message}`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Archive error: ${err.message}`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Assignment ${code} deleted.`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Assignment error: ${err.message}`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Assignment list error: ${err.message}`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Delete attempt error: ${err.message}`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Delete error: ${err.message}`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Deleted ${who}'s attempt.`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Deleting selected attempts… (${deleted}/${n})`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Grade error: ${err.message}`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Graded ${it.studentName || 'student'} · Q${gradingFocusState.qIndex + 1} · ${Number(points || 0)} pts.`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Graded Q${Number(it.qIndex || 0) + 1} for ${safeAttemptId}.`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Graded Q${Number(it.qIndex || 0) + 1} for ${studentGradingFocusState.studentName} · ${Number(points || 0)} pts.`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Graded Q${Number(qIndex) + 1} · ${attemptId}.`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Open quiz error: ${err.message}`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Reopen error: ${err.message}`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Reopened attempt ${attemptId}.`;",
  "app.js\u0000if (assignmentStatusEl) assignmentStatusEl.textContent = `Toggle error: ${err.message}`;",
  "app.js\u0000if (hostAttemptsSummaryEl) hostAttemptsSummaryEl.textContent = `Attempt snapshot error: ${err.message}`;",
  "app.js\u0000if (liveProgressEl) liveProgressEl.textContent = `Progress: ${previewMode.index + 1} / ${quiz.questions.length}`;",
  "app.js\u0000if (liveResponsesEl) liveResponsesEl.textContent = `Answers this round: ${sim.hostState.responseCount} / ${sim.hostState.playerCount}`;",
  "app.js\u0000if (projectorAnswersEl) projectorAnswersEl.textContent = `👥 Answers: ${sim.hostState.responseCount} / ${sim.hostState.playerCount}`;",
  "app.js\u0000if (projectorAnswersEl) projectorAnswersEl.textContent = `👥 Answers: ${state.responseCount} / ${state.playerCount}`;",
  "app.js\u0000info.textContent = `Find ${required} wrong token(s), then rewrite.`;",
  "app.js\u0000joinProgressEl.textContent = `Question ${Math.max(0, state.currentIndex + 1)} / ${state.totalQuestions}`;",
  "app.js\u0000joinScoreEl.textContent = `Score: ${data.score}`;",
  "app.js\u0000joinScoreEl.textContent = `Score: ${state.score}`;",
  "app.js\u0000label.textContent = `Answer: ${String(it?.answerText || '') || '(blank)'}`;",
  "app.js\u0000li.textContent = `${i + 1}. ${p.name} - ${p.score} pts`;",
  "app.js\u0000line.textContent = `${entry.name}: ${formatHistoryAnswer(entry)} ${verdict}${entry.graded ? ` (${Number(entry.pointsAwarded || 0)} pts)` : ''}`;",
  "app.js\u0000livePhaseEl.textContent = `Phase: question${reason}`;",
  "app.js\u0000meta.textContent = `${String(a?.className || '').trim() || 'All classes'} · Attempts ${attemptsText} · ${dueText}`;",
  "app.js\u0000meta.textContent = `Completion: ${completion} (${answered}/${total}) · Accuracy: ${acc} · Pending teacher: ${pending}`;",
  "app.js\u0000metaSpan.textContent = `· ${ws.quizCount || 0} quizzes · created ${created} · expires ${expires}`;",
  "app.js\u0000missingNote.textContent = `Mark failed: ${err.message}`;",
  "app.js\u0000missingNote.textContent = `No email found for: ${missingNames.join(', ')} (check the roster sheet)`;",
  "app.js\u0000more.textContent = `Email: ${email} · Auto pts: ${Number(s.pointsAuto || 0)} · Teacher pts: ${Number(s.pointsTeacher || 0)} · Last answer: ${lastAnswerText} · Events: ${Number(s.eventCount || 0)}`;",
  "app.js\u0000name.textContent = `${p.name}${idSuffix}${userSuffix} - ${p.score} pts${p.answeredCurrent ? ' [answered]' : ''}`;",
  "app.js\u0000out.textContent = `${slider.value}${q.unit ? ` ${escapeHtml(q.unit)}` : ''}`;",
  "app.js\u0000out.textContent = `Selected: ${slider.value}${question.unit ? ` ${question.unit}` : ''}`;",
  "app.js\u0000overwriteLabel.textContent = `I have reviewed the ${includedOverwrite.length} overwrite${includedOverwrite.length === 1 ? '' : 's'}`;",
  "app.js\u0000progressEl.innerHTML = `<strong>Done.</strong> ${done} applied${failed ? `, ${failed} failed` : ''}.${failures.length ? `<details style=\"margin-top:6px;\"><summary>Show failures</summary><pre style=\"white-space:pre-wrap;font-size:0.8rem;\">${escapeHtml(failures.join('\\n'))}</pre></details>` : ''}`;",
  "app.js\u0000progressEl.textContent = `Applying ${done + 1} of ${rowsToApply.length}…`;",
  "app.js\u0000progressEl.textContent = `Question ${soloGame.index + 1} / ${quiz.questions.length}`;",
  "app.js\u0000projectorTimerEl.textContent = `⏱️ Time: ${limitSec}s`;",
  "app.js\u0000projectorTimerEl.textContent = `⏱️ Time: ${remaining}s`;",
  "app.js\u0000right.textContent = `${score} pts`;",
  "app.js\u0000safeBtn.textContent = `Apply ${n} grade${n === 1 ? '' : 's'}`;",
  "app.js\u0000scoreEl.textContent = `${totalScore} pts`;",
  "app.js\u0000scoreEl.textContent = `Score: ${soloGame.score}`;",
  "app.js\u0000selectedCountEl.textContent = `${notifySelection.ids.size} selected`;",
  "app.js\u0000setStatus(feedbackEl, `${label} · ${sign} pts · ${result.hint || ''}`.trim(), pts >= 0 ? 'ok' : 'bad');",
  "app.js\u0000splash.textContent = `🎯 ${points} points`;",
  "app.js\u0000status.textContent = `${recipients.length} recipient(s) resolved · ${attempts.length} attempt(s) selected`;",
  "app.js\u0000status.textContent = `Found ${items.length} GIFs. Click one to use.`;",
  "app.js\u0000status.textContent = `Found ${items.length} images. Click an image to import.`;",
  "app.js\u0000status.textContent = `Looking up emails for ${attempts.length} student(s)...`;",
  "app.js\u0000statusEl.innerHTML = `⚠ Unrecognized language code \"<strong>${escapeHtml(raw)}</strong>\". ${renderRecognitionLangStatus(status)}`;",
  "app.js\u0000studentPreviewSummaryEl.textContent = `Summary: ${submitted}/${list.length} submitted · ${correct} correct · ${wrong} wrong · ${none} no submission · ${bets} with bet · phase: ${phase} · seed:${qSeed}`;",
  "app.js\u0000tEl.innerHTML = `<em>Transcript:</em> ${escapeHtml(transcript)}`;",
  "app.js\u0000title.textContent = `Notify students — ${quizTitle}`;",
  "app.js\u0000top.innerHTML = `<strong>${escapeHtml(name)}${escapeHtml(classSuffix)}</strong> · ${Number(s.scoreCurrent || 0)} pts · ${signal} ${accuracy}`;",
  "app.js\u0000window.alert(`⚠️ Orphan purge aborted (nothing was deleted).\\n\\n${msg}\\n\\nThis usually means the Durable Objects daily row-read quota is exhausted — the purge needs to load all live assignments to know what's safe to delete. Retry after UTC midnight when the quota resets.`);",
  "app.js\u0000window.alert(`⚠️ Partial purge.\\n\\nDurable Objects purge FAILED:\\n  ${data.doError}\\n\\nR2 cleanup ran: ${r2Msg}\\n\\nThe DO part needs DO row reads, which are blocked when the daily quota is exhausted. Retry after UTC midnight.`);",
  "app.js\u0000window.alert(`🗑️ Orphan media purge done.\\n\\nLive assignments scanned: ${data.liveAssignments || 0}\\nAlive R2 prefixes: ${data.alivePrefixes || 0}\\nFolders deleted: ${data.deletedFolders || 0}\\nObjects deleted: ${data.deletedRows || 0}\\n\\n(If \"Folders deleted\" is 0, it means every assign-*/ folder is still referenced by a current assignment — no leaks to clean. The 24h average storage metric on the R2 dashboard lags real state by hours.)`);",
  "app.js\u0000window.alert(`🧹 Preview backlog purged.\\n\\n${doMsg}\\n${r2Msg}\\n\\n(R2 dashboard \"Storage\" metric is a 24h rolling average — it can take hours to reflect this drop. Check the Objects tab for live state.)`);",
  "play.js\u0000bg.textContent = 'Danger';",
  "play.js\u0000if (out) out.textContent = `${slider.value}${question.unit ? ` ${escapeHtml(question.unit)}` : ''}`;",
  "play.js\u0000li.textContent = `${i + 1}. ${p.name} - ${p.score} pts`;",
  "play.js\u0000out.textContent = `${slider.value}${question.unit ? ` ${escapeHtml(question.unit)}` : ''}`;",
  "play.js\u0000scoreSummary.textContent = `${percent}% · ${totalPoints.toLocaleString()} pts${pendingTxt}`;",
  "play.js\u0000transcriptEl.innerHTML = `“<strong>${escapeHtml(final)}</strong>${interim ? ` <span class=\"muted\">${escapeHtml(interim)}</span>` : ''}”`;",
  "question-bank-ui.js\u0000flashOk('Unlock teacher access first, then try Sync again.');",
  "question-bank-ui.js\u0000flashOk(`Added ${added} questions${skipped ? ` (${skipped} skipped)` : ''}.`);"
]
